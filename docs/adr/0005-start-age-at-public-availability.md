# Start release age at public availability

Release Age starts on the first date an ordinary customer can use a Flagship Model through an official product or API without a private invitation. This excludes announcement-only and invite-only dates but includes paid access and openly accessible previews, keeping the age tied to a model people can actually experience rather than to marketing or a later stable label.
